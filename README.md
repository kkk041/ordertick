# OrderTick

OrderTick 是一款面向接单场景的 Electron 桌面计时器，适合陪玩、代练、陪练等按时计费的工作流。它把实时计时、订单台账、历史查询、实际到手统计和收入分析整合到一个桌面应用里。

## 简介

OrderTick 专注解决按时接单时最常见的几件事：开单时快速计时，结束后自动结算，遇到 15 分钟制或老板实付与系统结算不一致时可手动修正实际到手金额，并在历史订单和数据看板中统一展示最终收入结果。

## 主要功能

- 实时开始接单、结束接单与本单时长展示
- 分钟制与 15 分钟制计费规则
- 按比例抽成与按小时固定抽成
- 实际到手金额手动覆盖
- 今日订单台账与历史订单查询
- Excel 导入、导出与模板下载
- 收入分析、趋势图和老板维度统计
- 亮色、暗色、少女模式主题切换

## 技术栈

- Electron
- React 18
- Vite
- Ant Design
- XLSX

## 本地开发

```bash
npm install
npm run dev
```

## 打包

```bash
npm run build
```

打包产物会输出到 `release` 目录。

## 回归测试清单（v1.3.0）

下面这份清单用于每次发布前做完整流程验证，覆盖你当前最核心的接单、补录、结算和配置场景。

| 模块     | 用例                          | 预期结果                                |
| -------- | ----------------------------- | --------------------------------------- |
| 接单计时 | 开始接单 -> 结束接单          | 生成已完成订单，时长/结算/抽成/到手正常 |
| 接单计时 | 订单标记为未结算              | 订单显示未结，未结金额累计增加          |
| 接单计时 | 订单标记为已结算              | 订单显示已结，已结金额累计增加          |
| 接单计时 | 输入接单群                    | 订单保存并在历史中可见接单群            |
| 接单计时 | 选择计费模板后开单            | 自动带入模板计费规则与抽成方式          |
| 接单计时 | 关闭“今日打气”                | 总览不展示今日打气卡片                  |
| 接单计时 | 开启“今日打气”                | 总览展示今日打气卡片                    |
| 补录订单 | 补录并选择未结算              | 历史记录显示未结且可后续改已结          |
| 补录订单 | 补录并选择已结算              | 历史记录显示已结并记录结算时间          |
| 快速补录 | 快速计算补入 + 选择模板       | 成功生成订单并继承模板配置              |
| 历史订单 | 关键词检索（老板/备注/群）    | 能正确过滤对应订单                      |
| 历史订单 | 按结算状态筛选                | 全部/仅已结/仅未结筛选正确              |
| 历史订单 | 单条改已结/改未结             | 订单状态切换成功，金额统计同步          |
| 历史订单 | 批量改已结                    | 选中多条后全部标记为已结                |
| 历史订单 | 批量改未结                    | 选中多条后全部标记为未结                |
| 历史订单 | 批量删除                      | 选中订单全部删除                        |
| 历史订单 | 导出 Excel                    | 导出含接单群/计费模板/结算状态          |
| 历史订单 | 下载导入模板                  | 模板含接单群/计费模板/结算状态列        |
| 历史订单 | 导入 Excel 预览并确认         | 有效数据导入成功，无效行被忽略并提示    |
| 系统设置 | 新增/编辑计费模板             | 模板可保存并在接单页使用                |
| 系统设置 | 修改未结提醒配置（1/2/3天）   | 超期未结提醒阈值按配置生效              |
| 系统设置 | 切换提醒口径（自然日/24小时） | 提醒触发时间口径变化正确                |
| 系统设置 | 设置最少未结单数触发          | 未达到门槛不提醒，达到后提醒            |
| 更新日志 | 查看更新日志页                | 可看到 v1.3.0 更新内容                  |

### 最近一次执行结果

执行日期：2026-04-20

| 检查项        | 命令/方式                                                                                                                                                                       | 结果                       |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- |
| 核心文件 lint | `npx eslint src/pages/dashboard/OverviewPage.jsx src/pages/dashboard/HistoryOrdersPage.jsx src/pages/system/AppSettingsPage.jsx src/utils/pricing.js src/utils/orderStorage.js` | 通过                       |
| 全量 lint     | `npm run lint`                                                                                                                                                                  | 通过（本次已清理到可通过） |
| 构建          | `npm run build:renderer`                                                                                                                                                        | 通过                       |
